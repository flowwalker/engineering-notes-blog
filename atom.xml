<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>flowwalker之工巧Blog</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>https://flowwalker.github.io/engineering-notes-blog/</id>
  <link href="https://flowwalker.github.io/engineering-notes-blog/" rel="alternate"/>
  <link href="https://flowwalker.github.io/engineering-notes-blog/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, flowwalker之工巧Blog</rights>
  <subtitle>于浩歌狂热之际饮冰寒凉</subtitle>
  <title>工程笔记</title>
  <updated>2026-07-23T10:31:14.277Z</updated>
  <entry>
    <author>
      <name>flowwalker之工巧Blog</name>
    </author>
    <category term="博客" scheme="https://flowwalker.github.io/engineering-notes-blog/categories/%E5%8D%9A%E5%AE%A2/"/>
    <category term="Hexo" scheme="https://flowwalker.github.io/engineering-notes-blog/tags/Hexo/"/>
    <category term="Obsidian" scheme="https://flowwalker.github.io/engineering-notes-blog/tags/Obsidian/"/>
    <category term="Quartz" scheme="https://flowwalker.github.io/engineering-notes-blog/tags/Quartz/"/>
    <category term="GitHub Pages" scheme="https://flowwalker.github.io/engineering-notes-blog/tags/GitHub-Pages/"/>
    <content>
      <![CDATA[<p>最近重新规划了整套博客体系，从原来单一的主博客扩展为”主博客 + 4 个学科子博客 + 1 个 Wiki”的结构。这篇文章记录整体思路和关键实现步骤。</p><h2 id="1-主博客：flowwalker-top"><a href="#1-主博客：flowwalker-top" class="headerlink" title="1. 主博客：flowwalker.top"></a>1. 主博客：flowwalker.top</h2><p>主博客是我最早搭建的网站，使用 <strong>Hexo + anzhiyu 主题</strong>，部署在 GitHub Pages 上，绑定自定义域名 <code>flowwalker.top</code>。anzhiyu 主题功能比较丰富，目前包含：</p><ul><li>文章归档、分类、标签</li><li>相册集</li><li>追番页</li><li>音乐页面（Taylor 音乐馆、随机乐音乐厅）</li><li>随笔（触景随谈）</li><li>友链</li><li>TodoList</li></ul><p>我主要负责维护主博客，核心定位是综合性的生活记录和技术杂谈。</p><h2 id="2-整体架构"><a href="#2-整体架构" class="headerlink" title="2. 整体架构"></a>2. 整体架构</h2><p>随着课程笔记越来越多，把所有内容都堆在主博客里会变得混乱。于是把不同学科拆出来，形成独立的子博客：</p><p>每个子博客有独立的 URL、独立的主题配置，以后想单独公开或转让也方便。</p><pre><code class="hljs text">flowwalker.github.io/                  # 主博客├── math-notes-blog/             # 数学博客├── physics-notes-blog/          # 物理博客├── coding-notes-blog/           # 编程博客├── engineering-notes-blog/      # 工程笔记└── flowwalker-wiki/             # 知识库</code></pre><p>主博客的菜单里已经加了所有子站和 Wiki 的入口，形成一个整体的导航系统。</p><h2 id="3-子博客的技术实现"><a href="#3-子博客的技术实现" class="headerlink" title="3. 子博客的技术实现"></a>3. 子博客的技术实现</h2><h3 id="3-1-仓库结构"><a href="#3-1-仓库结构" class="headerlink" title="3.1 仓库结构"></a>3.1 仓库结构</h3><p>每个子博客采用两套仓库：</p><ul><li><strong>Public 部署仓库</strong>：只存放生成的 HTML，用于 GitHub Pages 访问</li><li><strong>Private 源文件备份仓库</strong>：存放 Markdown 源文件、配置文件、主题等</li></ul><p>我最后选择了 <strong>4 个 Public 部署仓库 + 1 个统一 Private 备份仓库 <code>notes-source</code></strong> 的方案。这样只需要管理 5 个仓库，而不是 8 个。</p><h3 id="3-2-本地目录"><a href="#3-2-本地目录" class="headerlink" title="3.2 本地目录"></a>3.2 本地目录</h3><pre><code class="hljs text">/Users/focus/Desktop/blog/notes/├── hexo-dawn-template-main/   # 模板母版├── math-notes-blog/├── physics-notes-blog/├── coding-notes-blog/└── engineering-notes-blog/</code></pre><h3 id="3-3-主题配置"><a href="#3-3-主题配置" class="headerlink" title="3.3 主题配置"></a>3.3 主题配置</h3><p>子博客使用朋友基于 Icarus 修改的 <strong>Dawn</strong> 主题。它本身已经做了比较干净的配置：</p><ul><li>社交链接只保留 RSS</li><li>Giscus 评论关闭</li><li>百度统计关闭</li><li>菜单：首页、归档、分类、标签</li></ul><p>仅需修改每个子博客的 <code>_config.yml</code>：</p><pre><code class="hljs yaml"><span class="hljs-attr">title:</span> <span class="hljs-string">数学博客</span><span class="hljs-attr">subtitle:</span> <span class="hljs-string">数学物理方法</span> <span class="hljs-string">·</span> <span class="hljs-string">复变函数</span> <span class="hljs-string">·</span> <span class="hljs-string">概率论</span><span class="hljs-attr">url:</span> <span class="hljs-string">https://flowwalker.github.io/math-notes-blog</span><span class="hljs-attr">root:</span> <span class="hljs-string">/math-notes-blog/</span><span class="hljs-attr">deploy:</span>  <span class="hljs-attr">type:</span> <span class="hljs-string">git</span>  <span class="hljs-attr">repo:</span> <span class="hljs-string">git@github.com:flowwalker/math-notes-blog.git</span>  <span class="hljs-attr">branch:</span> <span class="hljs-string">main</span></code></pre><p><code>url</code> 和 <code>root</code> 必须按仓库名设置，否则生成的链接会出错。因为 GitHub Pages 项目站的地址是 <code>https://flowwalker.github.io/&lt;仓库名&gt;/</code>。</p><blockquote><p>注：数学博客现已迁移到 Vercel 部署，通过自定义域名 <a href="https://math.flowwalker.top/">math.flowwalker.top</a> 访问，<code>_config.yml</code> 相应改为 <code>url: https://math.flowwalker.top</code>、<code>root: /</code>。</p></blockquote><h3 id="3-4-部署"><a href="#3-4-部署" class="headerlink" title="3.4 部署"></a>3.4 部署</h3><p>安装 <code>hexo-deployer-git</code> 后，每个子博客用：</p><pre><code class="hljs bash">npx hexo clean &amp;&amp; npx hexo generate &amp;&amp; npx hexo deploy</code></pre><h3 id="3-5-一个坑：GitHub-Pages-触发"><a href="#3-5-一个坑：GitHub-Pages-触发" class="headerlink" title="3.5 一个坑：GitHub Pages 触发"></a>3.5 一个坑：GitHub Pages 触发</h3><p>首次在 GitHub 里开启 Pages 后，网站不会自动发布。必须在<strong>开启之后</strong>再推一次新提交，GitHub 才会触发构建。这个坑在子博客部署时浪费了不少时间，解决方式是推一个空提交：</p><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> .deploy_git &amp;&amp; git commit --allow-empty -m <span class="hljs-string">&quot;trigger&quot;</span> &amp;&amp; git push</code></pre><h2 id="4-Wiki：Obsidian-Quartz的技术实现"><a href="#4-Wiki：Obsidian-Quartz的技术实现" class="headerlink" title="4. Wiki：Obsidian + Quartz的技术实现"></a>4. Wiki：Obsidian + Quartz的技术实现</h2><h3 id="4-1-为什么用-Wiki？"><a href="#4-1-为什么用-Wiki？" class="headerlink" title="4.1 为什么用 Wiki？"></a>4.1 为什么用 Wiki？</h3><p>课程笔记里有很多概念需要互相引用。传统的博客按文章组织，很难表达这种网状关系。</p><p>此外最近上手Obsidian, 也想使用新工具进行创作</p><p>Obsidian + Quartz 组合正好解决这个问题：</p><ul><li><strong>Obsidian</strong> 负责本地写作，支持 <code>[[双向链接]]</code></li><li><strong>Quartz 4</strong> 把 Obsidian 笔记转换成静态网站</li><li>发布到 GitHub Pages，支持搜索、反链、知识图谱</li></ul><h3 id="4-2-本地结构"><a href="#4-2-本地结构" class="headerlink" title="4.2 本地结构"></a>4.2 本地结构</h3><pre><code class="hljs text">~/Desktop/blog/wiki/├── content/          # Obsidian Vault，也是 Quartz 的笔记目录│   ├── index.md│   └── ...├── quartz.config.ts├── quartz.layout.ts└── .github/workflows/deploy.yml</code></pre><p><strong>关键</strong>：Obsidian 的 Vault 必须打开 <code>wiki/content/</code> 这个文件夹，而不是 <code>wiki/</code> 根目录。否则笔记会落在错误位置，发布不出去。</p><h3 id="4-3-Quartz-配置"><a href="#4-3-Quartz-配置" class="headerlink" title="4.3 Quartz 配置"></a>4.3 Quartz 配置</h3><p><code>quartz.config.ts</code> 主要修改：</p><pre><code class="hljs typescript"><span class="hljs-attr">pageTitle</span>: <span class="hljs-string">&quot;flowwalker 的知识库&quot;</span>,<span class="hljs-attr">locale</span>: <span class="hljs-string">&quot;zh-CN&quot;</span>,<span class="hljs-attr">baseUrl</span>: <span class="hljs-string">&quot;flowwalker.github.io/flowwalker-wiki&quot;</span>,<span class="hljs-attr">analytics</span>: <span class="hljs-literal">null</span>,</code></pre><p>字体则通过 <code>quartz/styles/custom.scss</code> 覆盖：</p><pre><code class="hljs scss"><span class="hljs-selector-pseudo">:root</span><span class="hljs-selector-pseudo">:root</span> &#123;  <span class="hljs-attr">--titleFont</span>: <span class="hljs-string">&quot;Comic Neue&quot;</span>, ...;  <span class="hljs-attr">--headerFont</span>: <span class="hljs-string">&quot;Comic Neue&quot;</span>, ...;  <span class="hljs-attr">--bodyFont</span>: <span class="hljs-string">&quot;Comic Neue&quot;</span>, ...;&#125;</code></pre><p>这里用 <code>:root:root</code> 提高优先级，覆盖 Quartz 在末尾追加的 <code>:root</code> 变量定义。</p><h3 id="4-4-部署"><a href="#4-4-部署" class="headerlink" title="4.4 部署"></a>4.4 部署</h3><p>Wiki 使用 GitHub Actions 自动构建：</p><ol><li>在 Obsidian 里写笔记</li><li><code>git push</code> 到 <code>flowwalker/flowwalker-wiki</code></li><li>Actions 自动运行 <code>npm ci</code> 和 <code>npx quartz build</code></li><li>构建产物通过 <code>actions/deploy-pages</code> 发布到 Pages</li></ol><h3 id="4-5-双向链接"><a href="#4-5-双向链接" class="headerlink" title="4.5 双向链接"></a>4.5 双向链接</h3><p>在 Obsidian 里写：</p><pre><code class="hljs markdown">[[傅里叶变换]] 在信号与系统中非常重要。</code></pre><p>Quartz 会自动解析这个链接，生成对应的页面和关系图。这是 Wiki 相比普通博客最大的优势。</p><h2 id="5-来路方长"><a href="#5-来路方长" class="headerlink" title="5. 来路方长"></a>5. 来路方长</h2><p>这次重构最主要做的是分清了三类内容：</p><ul><li><p><strong>主博客</strong>：综合、生活、随笔</p></li><li><p><strong>子博客</strong>：按学科组织的课程笔记</p></li><li><p><strong>Wiki</strong>：概念密集、需要互相链接的知识库或者经验经历</p></li></ul><hr><p>工具链上：</p><ul><li><p><strong>GitHub Pages</strong> 负责托管</p></li><li><p><strong>Obsidian</strong> 和 <strong>Typora</strong> 负责写作</p></li><li><p><strong>Hexo</strong> 负责博客</p></li><li><p><strong>Quartz</strong> 负责 Wiki</p></li></ul><hr><p>下一步可能会为子博客配置评论，或者给 Wiki 增加更多笔记。</p>]]>
    </content>
    <id>https://flowwalker.github.io/engineering-notes-blog/posts/df65/</id>
    <link href="https://flowwalker.github.io/engineering-notes-blog/posts/df65/"/>
    <published>2026-07-19T18:00:00.000Z</published>
    <summary>合抱之木，生于毫末。</summary>
    <title>我的博客系统搭建记录：主博客、子博客与 Wiki</title>
    <updated>2026-07-23T10:31:14.277Z</updated>
  </entry>
  <entry>
    <author>
      <name>flowwalker之工巧Blog</name>
    </author>
    <category term="站务" scheme="https://flowwalker.github.io/engineering-notes-blog/categories/%E7%AB%99%E5%8A%A1/"/>
    <category term="开篇" scheme="https://flowwalker.github.io/engineering-notes-blog/tags/%E5%BC%80%E7%AF%87/"/>
    <content>
      <![CDATA[<p>这里是我的工程学习笔记博客。</p><p>公式用 MathJax 渲染：</p><p>$$X(f) = \int_{-\infty}^{\infty} x(t)\, e^{-j2\pi f t}\, dt$$</p><p>在这个时代的潮流中，虽然手搓已经失去了效率一说，权威也被解构，那么——</p><p>似乎一切的自我陶醉都是一种自作多情，但</p><p>f又何妨呢？</p><p>心中拥有美好，荒岛也能耕种出繁花～</p><p>孤芳自赏者，毋为世事倾侧。</p><p>主博客请访问 <a href="https://flowwalker.github.io/">flowwalker.github.io</a>。</p>]]>
    </content>
    <id>https://flowwalker.github.io/engineering-notes-blog/posts/3b26/</id>
    <link href="https://flowwalker.github.io/engineering-notes-blog/posts/3b26/"/>
    <published>2026-07-19T15:30:00.000Z</published>
    <summary>纸上终浅，何不躬行?</summary>
    <title>工程博客开张</title>
    <updated>2026-07-21T12:55:15.485Z</updated>
  </entry>
  <entry>
    <author>
      <name>flowwalker之工巧Blog</name>
    </author>
    <category term="自动化" scheme="https://flowwalker.github.io/engineering-notes-blog/categories/%E8%87%AA%E5%8A%A8%E5%8C%96/"/>
    <category term="launchd" scheme="https://flowwalker.github.io/engineering-notes-blog/tags/launchd/"/>
    <category term="shell" scheme="https://flowwalker.github.io/engineering-notes-blog/tags/shell/"/>
    <category term="macOS" scheme="https://flowwalker.github.io/engineering-notes-blog/tags/macOS/"/>
    <category term="Hexo" scheme="https://flowwalker.github.io/engineering-notes-blog/tags/Hexo/"/>
    <category term="自动化" scheme="https://flowwalker.github.io/engineering-notes-blog/tags/%E8%87%AA%E5%8A%A8%E5%8C%96/"/>
    <content>
      <![CDATA[<h1 id="利用-launchd-实现-Hexo-博客定时自动推送"><a href="#利用-launchd-实现-Hexo-博客定时自动推送" class="headerlink" title="利用 launchd 实现 Hexo 博客定时自动推送"></a>利用 launchd 实现 Hexo 博客定时自动推送</h1><h3 id="零、-选择工具：Crontab-vs-Launchd"><a href="#零、-选择工具：Crontab-vs-Launchd" class="headerlink" title="零、 选择工具：Crontab vs Launchd"></a>零、 选择工具：Crontab vs Launchd</h3><p>在 macOS 上实现自动化推送，通常有两种主流选择：</p><ul><li><p>crontab</p></li><li><p>Launchd</p></li></ul><blockquote><p><strong>1. Crontab (传统的 Unix 定时任务)</strong><br><strong>操作指令：</strong></p><ol><li>在终端输入 <code>crontab -e</code> 进入编辑界面。</li><li>添加一行配置（例如每天 22:30 执行）：<br><code>30 22 * * * /bin/bash /Users/focus/Library/LaunchAgents/auto_push.sh &gt;&gt; /tmp/hexo_cron.log 2&gt;&amp;1</code></li><li>保存并退出即可。</li></ol><p><strong>特点：</strong> 语法极其简单，配置迅速。但它的致命缺点是<strong>不支持“补跑”</strong>。如果 22:30 你的 Mac 盖上了盖子处于休眠状态，crontab 任务就会直接跳过，直到第二天同一时间。</p></blockquote><blockquote><p><strong>2. Launchd (Apple 官方)</strong><br><strong>特点：</strong> 虽然 <code>.plist</code> 的 XML 格式比 crontab 复杂，但其拥有 <strong>StartCalendarInterval</strong> 机制。如果设定时间点电脑在休眠，<code>launchd</code> 会在系统唤醒后<strong>立即补跑</strong>任务。对于笔记备份和博客推送这种“必须成功”的任务，<code>launchd</code> 是更可靠的选择。</p></blockquote><h2 id="一、确认环境"><a href="#一、确认环境" class="headerlink" title="一、确认环境"></a>一、确认环境</h2><p>在编写脚本前，需在终端执行以下诊断，确保命令在“纯净环境”下依然有效。</p><h3 id="1-确认命令的物理路径"><a href="#1-确认命令的物理路径" class="headerlink" title="1. 确认命令的物理路径"></a>1. 确认命令的物理路径</h3><p><code>launchd</code> 不识别 <code>hexo</code> 或 <code>git</code> 这种简写，需提供绝对路径。</p><ul><li><strong>输入指令</strong>：<code>which hexo</code></li><li><strong>预期结果</strong>：记录返回的路径（如 <code>/opt/homebrew/bin/hexo</code>）。</li><li>在脚本中替换所有 <code>hexo</code> 字样，防止 <code>command not found</code>。</li></ul><h3 id="2-确认环境变量-PATH"><a href="#2-确认环境变量-PATH" class="headerlink" title="2. 确认环境变量 $PATH"></a>2. 确认环境变量 $PATH</h3><p>脚本需要知道去哪里寻找执行引擎（如 Node.js）。</p><ul><li><strong>输入指令</strong>：<code>echo $PATH</code></li><li><strong>预期结果</strong>：一串由冒号分隔的路径（如 <code>/opt/homebrew/bin:/usr/local/bin...</code>）。</li><li>将这串路径复制，写在脚本开头的 <code>export PATH=&quot;...&quot;</code> 后面。</li></ul><h3 id="3-确认-SSH-密钥授权状态"><a href="#3-确认-SSH-密钥授权状态" class="headerlink" title="3. 确认 SSH 密钥授权状态"></a>3. 确认 SSH 密钥授权状态</h3><p>后台运行无法弹出密码框，必须确认密钥已托管给系统。</p><ul><li><strong>输入指令</strong>：<code>ssh-add -l</code></li><li><strong>预期结果</strong>：看到密钥指纹。如果显示 <code>The agent has no identities</code>，则需要在脚本中手动加载。</li><li>手动执行 <code>hexo d</code>。如果<strong>无需</strong>输入密码即可推送，则配置正确。</li></ul><h3 id="二、自动化-Shell-脚本"><a href="#二、自动化-Shell-脚本" class="headerlink" title="二、自动化 Shell 脚本"></a>二、自动化 Shell 脚本</h3><p>我们需要创建一个封装脚本，手动重构环境变量并定位物理路径。</p><p><strong>脚本位置：</strong> <code>~/Library/LaunchAgents/auto_push.sh</code></p><pre><code class="hljs bash"><span class="hljs-meta">#!/bin/bash</span><span class="hljs-comment"># 1. 显式声明 PATH，包含 Homebrew 和 Conda 路径</span><span class="hljs-built_in">export</span> PATH=<span class="hljs-string">&quot;/Users/focus/miniconda3/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:<span class="hljs-variable">$PATH</span>&quot;</span><span class="hljs-comment"># 2. 加载 SSH 密钥（关键：解决后台运行无身份认证问题）</span>ssh-add --apple-use-keychain ~/.ssh/id_rsa 2&gt;/dev/null<span class="hljs-comment"># 3. 业务逻辑</span>BLOG_DIR=<span class="hljs-string">&quot;/Users/focus/WhereYourBlogIs&quot;</span>DINGTALK_URL=<span class="hljs-string">&quot;https://oapi.dingtalk.com/robot/send?access_token=YOUR_TOKEN&quot;</span><span class="hljs-built_in">cd</span> <span class="hljs-string">&quot;<span class="hljs-variable">$BLOG_DIR</span>&quot;</span> || <span class="hljs-built_in">exit</span> <span class="hljs-comment">#前者执行不了就退出</span><span class="hljs-comment"># 检索过去 24 小时内更新的文章</span>CHANGES=$(/usr/bin/find -L <span class="hljs-string">&quot;/Users/focus/Desktop/blog/my_blog/source/_posts/&quot;</span> \    -<span class="hljs-built_in">type</span> f -mmin -1560 -name <span class="hljs-string">&quot;*.md&quot;</span> | /usr/bin/sed <span class="hljs-string">&quot;s|.*/_posts/|- |&quot;</span>)[ -z <span class="hljs-string">&quot;<span class="hljs-variable">$CHANGES</span>&quot;</span> ] &amp;&amp; CONTENT=<span class="hljs-string">&quot;中哥,今日无新文章推送。&quot;</span> || CONTENT=<span class="hljs-string">&quot;中哥，今日新推送文章：\n<span class="hljs-variable">$CHANGES</span>&quot;</span><span class="hljs-comment"># 4. 执行 Hexo 流程</span>/opt/homebrew/bin/hexo cl &amp;&amp; /opt/homebrew/bin/hexo g &amp;&amp; /opt/homebrew/bin/hexo d<span class="hljs-comment"># 5. 钉钉机器人回传结果</span><span class="hljs-keyword">if</span> [ $? -eq 0 ]; <span class="hljs-keyword">then</span>    curl -s <span class="hljs-string">&quot;<span class="hljs-variable">$DINGTALK_URL</span>&quot;</span> -H <span class="hljs-string">&#x27;Content-Type: application/json&#x27;</span> \    -d <span class="hljs-string">&quot;&#123;\&quot;msgtype\&quot;: \&quot;text\&quot;, \&quot;text\&quot;: &#123;\&quot;content\&quot;: \&quot;<span class="hljs-variable">$CONTENT</span>\&quot;&#125;&#125;&quot;</span><span class="hljs-keyword">fi</span></code></pre><blockquote><p><strong>Shell 脚本逻辑框架极速学习</strong></p><table><thead><tr><th align="left">模块</th><th align="left">对应 Python 概念</th><th align="left">作用</th><th align="left">细节</th></tr></thead><tbody><tr><td align="left"><strong>#! &#x2F;bin&#x2F;bash</strong></td><td align="left">\</td><td align="left">告诉系统用哪个“翻译官”来读这个文件。</td><td align="left">必须放在第一行。</td></tr><tr><td align="left"><strong>export PATH</strong></td><td align="left"><code>os.environ[&#39;PATH&#39;]</code></td><td align="left"><strong>重构环境</strong>。让脚本能找到 <code>node</code>, <code>hexo</code> 等工具。</td><td align="left">后台运行不读 <code>.zshrc</code>，必须手动声明。</td></tr><tr><td align="left"><strong>ssh-add</strong></td><td align="left"><code>Authentication</code></td><td align="left"><strong>免密授权</strong>。从系统钥匙串读取 Git 推送所需的私钥。</td><td align="left">后台运行无法手动输密码，需靠此指令。</td></tr><tr><td align="left"><strong>Variables</strong></td><td align="left"><code>Variable</code> (变量)</td><td align="left">存储路径、URL、检索结果。</td><td align="left">注意：等号两边<strong>不能有空格</strong>。</td></tr><tr><td align="left"><strong>$(command)</strong></td><td align="left"><code>subprocess.check_output</code></td><td align="left"><strong>命令替换</strong>。执行一个命令并把输出结果存入变量。</td><td align="left">如 <code>CHANGES=$(find...)</code>。</td></tr><tr><td align="left"><strong>$ (逻辑判断)</strong></td><td align="left"><code>if not CHANGES:</code></td><td align="left"><strong>短路逻辑</strong>。根据变量是否为空生成不同的通知文案。</td><td align="left"><code>[ -z &quot;$VAR&quot; ]</code> 是检查字符串是否为空。</td></tr><tr><td align="left"><strong>$? (状态码)</strong></td><td align="left"><code>try...except</code> 或 <code>return code</code></td><td align="left"><strong>捕获上一步状态</strong>。<code>0</code> 代表成功，非 <code>0</code> 代表报错。</td><td align="left"><code>if [ $? -eq 0 ]</code> 决定是否发钉钉消息。</td></tr><tr><td align="left"><strong>curl</strong></td><td align="left"><code>requests.post()</code></td><td align="left"><strong>网络请求</strong>。向钉钉 API 发送 JSON 数据。</td><td align="left">使用 <code>-d</code> 参数传递 JSON 字符串。</td></tr></tbody></table><blockquote><p><strong>三元运算符</strong>。</p><pre><code class="hljs bash">[ -z <span class="hljs-string">&quot;<span class="hljs-variable">$CHANGES</span>&quot;</span> ] &amp;&amp; CONTENT=<span class="hljs-string">&quot;A&quot;</span> || CONTENT=<span class="hljs-string">&quot;B&quot;</span></code></pre><ol><li><strong><code>[ -z &quot;$CHANGES&quot; ]</code></strong>：这是一个判断题。<code>-z</code> 表示 “Zero”，意思是“检查字符串长度是否为 0”。<ul><li>如果今天没写文章，<code>$CHANGES</code> 是空的，判断为 <strong>真(True)</strong>。</li><li>如果今天写了文章，判断为 <strong>假(False)</strong>。</li></ul></li><li><strong><code>&amp;&amp;</code> (逻辑与)</strong>：只有当前面的判断为 <strong>真</strong> 时，才执行。</li><li><strong><code>||</code> (逻辑或)</strong>：只有当前面的判断为 <strong>假</strong> 时，才执行。</li></ol><ul><li><strong>路径 A (没写文章)</strong>：<br>判断为真 → 触发 <code>&amp;&amp;</code> 后面的赋值 → 跳过 <code>||</code>。<br>结果：<code>CONTENT</code> 变成了“今日无新文章”。</li><li><strong>路径 B (写了文章)</strong>：<br>判断为假 → 跳过 <code>&amp;&amp;</code> → 触发 <code>||</code> 后面的赋值。<br>结果：<code>CONTENT</code> 变成了“今日新推送文章：…”。</li></ul></blockquote><blockquote><p><code>$()</code>先运行内部，然后命令替换</p></blockquote></blockquote><h2 id="三、配置调度：launchd-Plist-文件"><a href="#三、配置调度：launchd-Plist-文件" class="headerlink" title="三、配置调度：launchd Plist 文件"></a>三、配置调度：launchd Plist 文件</h2><p>在 <code>~/Library/LaunchAgents/</code> 下创建 <code>com.user.pushblog.plist</code>，定义触发时间。</p><h3 id="1-plist-基础结构"><a href="#1-plist-基础结构" class="headerlink" title="1. plist 基础结构"></a>1. <code>plist</code> 基础结构</h3><p><code>plist</code> 是 XML 格式的键值对列表。所有配置必须包裹在 <code>&lt;dict&gt;</code> 标签内。</p><p>XML</p><pre><code class="hljs XML"><span class="hljs-meta">&lt;?xml version=<span class="hljs-string">&quot;1.0&quot;</span> encoding=<span class="hljs-string">&quot;UTF-8&quot;</span>?&gt;</span><span class="hljs-meta">&lt;!DOCTYPE <span class="hljs-keyword">plist</span> <span class="hljs-keyword">PUBLIC</span> <span class="hljs-string">&quot;-//Apple//DTD PLIST 1.0//EN&quot;</span> <span class="hljs-string">&quot;http://www.apple.com/DTDs/PropertyList-1.0.dtd&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">plist</span> <span class="hljs-attr">version</span>=<span class="hljs-string">&quot;1.0&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">dict</span>&gt;</span>    <span class="hljs-tag">&lt;/<span class="hljs-name">dict</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">plist</span>&gt;</span></code></pre><hr><h3 id="2-参数表"><a href="#2-参数表" class="headerlink" title="2. 参数表"></a>2. 参数表</h3><table><thead><tr><th><strong>键名 (Key)</strong></th><th><strong>数据类型</strong></th><th><strong>说明</strong></th><th><strong>示例</strong></th></tr></thead><tbody><tr><td><strong>Label</strong></td><td><code>string</code></td><td><strong>唯一身份标识</strong>。任务的“身份证号”，不可重复。</td><td><code>com.focus.blog</code></td></tr><tr><td><strong>ProgramArguments</strong></td><td><code>array</code></td><td><strong>执行指令序列</strong>。第一个值是解释器，第二个是脚本路径。</td><td><code>/bin/bash</code>, <code>/path/to/sh</code></td></tr><tr><td><strong>StartCalendarInterval</strong></td><td><code>dict</code></td><td><strong>定时触发器</strong>。设定具体的执行时间点。</td><td><code>Hour: 22</code>, <code>Minute: 30</code></td></tr><tr><td><strong>StandardOutPath</strong></td><td><code>string</code></td><td><strong>标准输出日志</strong>。保存脚本运行时的正常提示。</td><td><code>/tmp/blog.log</code></td></tr><tr><td><strong>StandardErrorPath</strong></td><td><code>string</code></td><td><strong>标准错误日志</strong>。保存脚本运行失败的报错信息。</td><td><code>/tmp/blog.err</code></td></tr></tbody></table><hr><h3 id="3-格式例子"><a href="#3-格式例子" class="headerlink" title="3. 格式例子"></a>3. 格式例子</h3><ul><li><p><strong>定点触发</strong>（每天 22:30）：</p><pre><code class="hljs XML"><span class="hljs-tag">&lt;<span class="hljs-name">key</span>&gt;</span>StartCalendarInterval<span class="hljs-tag">&lt;/<span class="hljs-name">key</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">dict</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">key</span>&gt;</span>Hour<span class="hljs-tag">&lt;/<span class="hljs-name">key</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">integer</span>&gt;</span>22<span class="hljs-tag">&lt;/<span class="hljs-name">integer</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">key</span>&gt;</span>Minute<span class="hljs-tag">&lt;/<span class="hljs-name">key</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">integer</span>&gt;</span>30<span class="hljs-tag">&lt;/<span class="hljs-name">integer</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">dict</span>&gt;</span></code></pre></li><li><p><strong>周期触发</strong>（每隔 1 小时&#x2F;3600 秒）：</p><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">key</span>&gt;</span>StartInterval<span class="hljs-tag">&lt;/<span class="hljs-name">key</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">integer</span>&gt;</span>3600<span class="hljs-tag">&lt;/<span class="hljs-name">integer</span>&gt;</span></code></pre></li></ul><hr><h3 id="4-增强功能"><a href="#4-增强功能" class="headerlink" title="4. 增强功能"></a>4. 增强功能</h3><ul><li><strong>RunAtLoad</strong>: <code>bool</code> 类型。设为 <code>&lt;true/&gt;</code> 则在任务加载或开机时立即执行一次。</li><li><strong>WorkingDirectory</strong>: <code>string</code> 类型。指定脚本运行时的根目录，免去脚本内 <code>cd</code> 的麻烦。</li><li><strong>KeepAlive</strong>: <code>bool</code> 类型。如果程序崩溃，系统会自动重启它（适用于常驻服务）。</li></ul><hr><h3 id="5-编写"><a href="#5-编写" class="headerlink" title="5. 编写"></a>5. 编写</h3><ol><li><p><strong>语法校验</strong>：编写完成后，使用终端指令检查 XML 格式是否合法：</p><p><code>plutil -lint ~/Library/LaunchAgents/your_filename.plist</code></p></li><li><p><strong>绝对路径原则</strong>：<code>plist</code> 内部<strong>禁止</strong>使用 <code>~</code> 或相对路径，必须使用全路径（如 <code>/Users/focus/...</code>）。</p></li><li><p><strong>生效逻辑</strong>：修改 <code>plist</code> 内容后，必须执行 <code>unload</code> 再 <code>load</code>，系统才会读取新配置。</p></li></ol><hr><h3 id="6-操作速查"><a href="#6-操作速查" class="headerlink" title="6. 操作速查"></a>6. 操作速查</h3><ul><li><strong>加载</strong>：<code>launchctl load [文件路径]</code></li><li><strong>卸载</strong>：<code>launchctl unload [文件路径]</code></li><li><strong>启动</strong>：<code>launchctl start [Label]</code></li><li><strong>停止</strong>：<code>launchctl stop [Label]</code></li></ul><h2 id="四、常用操作指令集"><a href="#四、常用操作指令集" class="headerlink" title="四、常用操作指令集"></a>四、常用操作指令集</h2><ul><li><p><strong>赋权脚本：</strong><br><code>chmod +x ~/Library/LaunchAgents/auto_push.sh</code></p></li><li><p><strong>加载任务：</strong><br><code>launchctl load ~/Library/LaunchAgents/com.user.pushblog.plist</code></p></li><li><p><strong>手动强制触发测试：(停止stop）</strong><br><code>launchctl start com.user.pushblog</code></p></li><li><p><strong>卸载任务（修改配置后必须执行）：</strong><br><code>launchctl unload ~/Library/LaunchAgents/com.user.pushblog.plist</code></p></li><li><p><strong>查看运行日志：</strong><br><code>tail -f /tmp/hexo_push.log</code></p></li><li><p>完整查看日志：</p><p><code> cat /tmp/hexo_push.log</code></p></li></ul><h2 id="Launchd-运维速查笔记-发疯记录"><a href="#Launchd-运维速查笔记-发疯记录" class="headerlink" title="Launchd 运维速查笔记(发疯记录)"></a>Launchd 运维速查笔记(发疯记录)</h2><h4 id="1-操作顺序（先下后上）"><a href="#1-操作顺序（先下后上）" class="headerlink" title="1. 操作顺序（先下后上）"></a>1. 操作顺序（先下后上）</h4><p><strong>原则</strong>：修改 <code>plist</code> 文件或脚本内容后，必须遵循“先卸载、再加载”的闭环，否则系统缓存会导致配置不生效或报错。</p><ol><li><strong>卸载 (Unload)</strong>：<code>launchctl unload ~/Library/LaunchAgents/com.user.pushblog.plist</code></li><li><strong>修改 (Modify)</strong>：编辑 <code>.plist</code> 或 <code>.sh</code> 文件。</li><li><strong>加载 (Load)</strong>：<code>launchctl load ~/Library/LaunchAgents/com.user.pushblog.plist</code></li><li><strong>测试 (Test)</strong>：<code>launchctl start com.user.pushblog</code>（手动触发一次验证逻辑）。</li></ol><hr><h4 id="2-报错处理：Load-failed-5-I-O-Error"><a href="#2-报错处理：Load-failed-5-I-O-Error" class="headerlink" title="2. 报错处理：Load failed: 5 (I&#x2F;O Error)"></a>2. 报错处理：Load failed: 5 (I&#x2F;O Error)</h4><ul><li><strong>现象</strong>：执行 <code>load</code> 时提示 <code>Input/output error</code>。</li><li><strong>病因</strong>：任务已在内存中加载，或在未卸载时修改了 <code>Label</code> 导致冲突。</li><li><strong>药方</strong>：<ul><li>先执行 <code>unload</code> 强行清理。</li><li>检查 XML 语法：<code>plutil -lint [路径]</code>。</li><li>若仍不行，多unload几次</li></ul></li></ul><hr><h4 id="3-静默卸载-2-dev-null"><a href="#3-静默卸载-2-dev-null" class="headerlink" title="3. 静默卸载 (2&gt;/dev/null)"></a>3. 静默卸载 (<code>2&gt;/dev/null</code>)</h4><ul><li><strong>用法</strong>：<code>launchctl unload [路径] 2&gt;/dev/null</code></li><li><strong>作用</strong>：将错误输出（Standard Error）重定向到不输出。</li><li><strong>场景</strong>：当你编写自动化脚本（如 <code>deploy.sh</code>）时，不确定任务是否已加载。加上这一句可以确保无论任务是否存在，都不会弹出干扰报错，直接执行后续的 <code>load</code>。</li></ul><hr><h4 id="4-状态检查：launchctl-list-grep-pushblog"><a href="#4-状态检查：launchctl-list-grep-pushblog" class="headerlink" title="4. 状态检查：launchctl list | grep pushblog"></a>4. 状态检查：<code>launchctl list | grep pushblog</code></h4><p>观察任务“活得好不好”。输出通常包含三列：</p><table><thead><tr><th><strong>PID (进程号)</strong></th><th><strong>Last Status (状态码)</strong></th><th><strong>Label (任务名)</strong></th></tr></thead><tbody><tr><td><code>12345</code></td><td><code>0</code></td><td><code>com.user.pushblog</code></td></tr><tr><td><code>-</code></td><td><code>0</code></td><td><code>com.user.pushblog</code></td></tr><tr><td><code>-</code></td><td><code>78</code></td><td><code>com.user.pushblog</code></td></tr></tbody></table><ul><li><strong>PID 为数字</strong>：说明脚本正在后台运行。</li><li><strong>PID 为 <code>-</code></strong>：说明任务已加载，正在等待定时触发。</li><li><strong>Status 为 <code>0</code></strong>：上次运行<strong>完美成功</strong>。</li><li><strong>Status 非 <code>0</code></strong>：上次运行<strong>失败</strong>（例如 <code>78</code> 通常代表路径错误或权限不足）。</li></ul><h4 id="5-核心Bug：macOS-隐私沙盒-FDA"><a href="#5-核心Bug：macOS-隐私沙盒-FDA" class="headerlink" title="5. 核心Bug：macOS 隐私沙盒 (FDA)"></a>5. 核心Bug：macOS 隐私沙盒 (FDA)</h4><p>这是本次任务“手动行，自动不行”的<strong>头号杀手</strong>。</p><ul><li><strong>现象</strong>：脚本在终端运行完美，但通过 <code>launchd</code> (plist) 运行时，<code>find</code> 无法读取 <code>Desktop</code> 目录下的文章。</li><li><strong>原理</strong>：后台进程默认被禁止访问用户敏感目录。即便脚本路径正确，系统也会静默拦截 <code>find</code> 的深入探测。</li><li><strong>对策</strong>：在 <strong>系统设置 -&gt; 隐私与安全性 -&gt; 完全磁盘访问权限</strong> 中，手动添加 <code>/bin/bash</code>（或你的脚本解释器）并开启开关。</li></ul><h4 id="6-工具差异：BSD-find-的“洁癖”"><a href="#6-工具差异：BSD-find-的“洁癖”" class="headerlink" title="6. 工具差异：BSD find 的“洁癖”"></a>6. 工具差异：BSD find 的“洁癖”</h4><p>macOS 自带的是 BSD 版 <code>find</code>，它比 Linux 的 GNU 版更固执。</p><ul><li><strong>坑点</strong>：不支持浮点数（如 <code>-mtime -1.1</code> 会报错 <code>bad unit</code>）。</li><li><strong>对策</strong>：改用分钟单位 <strong><code>-mmin</code></strong>。例如用 <code>-mmin -1560</code> 代替 <code>-mtime -1.1</code>，实现更精准、兼容性更好的时间窗口控制。</li></ul><h4 id="7-环境隔离：极简的-Shell-环境"><a href="#7-环境隔离：极简的-Shell-环境" class="headerlink" title="7. 环境隔离：极简的 Shell 环境"></a>7. 环境隔离：极简的 Shell 环境</h4><p><code>launchd</code> 运行脚本时不会加载你的个人配置文件（<code>.zshrc</code> 或 <code>.bash_profile</code>）。</p><ul><li><strong>坑点</strong>：缺少 <code>PATH</code> 可能导致找不到 <code>hexo</code>；缺少 <code>LANG</code> 可能导致处理中文路径时 <code>sed</code> 正则失效。</li><li><strong>对策</strong>：在脚本开头硬编码 <code>export PATH</code> 和 <code>export LANG=en_US.UTF-8</code>。</li></ul>]]>
    </content>
    <id>https://flowwalker.github.io/engineering-notes-blog/posts/2d48/</id>
    <link href="https://flowwalker.github.io/engineering-notes-blog/posts/2d48/"/>
    <published>2026-04-03T16:00:00.000Z</published>
    <summary>macOS 下利用 launchd 实现博客定时自动部署的完整记录</summary>
    <title>利用 launchd 实现 Hexo 博客定时自动推送</title>
    <updated>2026-07-26T12:01:27.621Z</updated>
  </entry>
</feed>
